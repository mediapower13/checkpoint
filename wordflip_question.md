Question: WordFlip — write a function WordFlip(str string) string that reverses the order of words in a string and returns the result followed by a newline. If the string is empty or contains only spaces return "Invalid Output" (or an empty line when specified). Ignore multiple spaces between words and trim leading/trailing spaces.

Expected function:
func WordFlip(str string) string {

}

Usage example:
fmt.Print(piscine.WordFlip("First second last")) // outputs: "last second First\n"

Answer file: wordflip.go
