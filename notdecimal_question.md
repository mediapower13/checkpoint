Question: NotDecimal — convert float string with decimal to int string removing dot (multiply by 10^n). If no decimal or only zeros after dot return original + newline. If empty return newline. If invalid number return original + newline.

Answer file: notdecimal.go
